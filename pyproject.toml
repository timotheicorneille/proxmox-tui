[build-system]
requires = ["setuptools>=68", "setuptools-scm>=8"]
build-backend = "setuptools.build_meta"

[project]
name = "proxmox-tui"
dynamic = ["version"]
requires-python = ">=3.11"
dependencies = [
    "textual>=0.70.0",
    "proxmoxer>=2.0.1",
    "requests>=2.31.0",
]

[project.scripts]
proxmox-tui = "proxmox_tui.main:main"

[tool.setuptools.packages.find]
where = ["."]
include = ["proxmox_tui*"]

[tool.setuptools_scm]
